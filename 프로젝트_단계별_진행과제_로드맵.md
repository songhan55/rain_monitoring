# 🚀 스마트 도로 위험 감지 & 내비게이션 알림 시스템: 단계별 진행 과제 로드맵

성공적인 캡스톤 프로젝트 완성을 위해 **기초 세팅부터 최종 발표 시연까지 총 7단계(Phase 0 ~ Phase 6)**로 체계화한 실행 로드맵입니다.

---

```mermaid
flowchart LR
    P0[Phase 0\nAPI & 환경 구축] --> P1[Phase 1\n기상청 트리거]
    P1 --> P2[Phase 2\nCCTV 영상 수집]
    P2 --> P3[Phase 3\nAI 모델 학습]
    P3 --> P4[Phase 4\n통합 백엔드 & DB]
    P4 --> P5[Phase 5\n웹 내비 & 시뮬레이터]
    P5 --> P6[Phase 6\n시연 & 심사 방어]
```

---

## 📌 [Phase 0] 사전 준비: 개발 환경 및 API 키 발급 (1주차)

### 1. 주요 과제
* [ ] **공공데이터포털 / 기상청 API허브 계정 가입 및 키 발급**
  * `기상청 단기예보(초단기실황조회)` API 활용 신청 (승인 즉시 또는 1~2일 소요)
* [ ] **CCTV API 키 발급**
  * 국가교통정보센터(ITS) `CCTV 영상 정보 Open API` 신청
  * (대안/보조) 서울시 TOPIS 교통정보 API 신청
* [ ] **지도/모빌리티 API 키 발급**
  * 카카오 개발자 센터 (`Kakao Maps JavaScript SDK`, `Kakao Mobility 길찾기 API`) 또는 SK Open API (`Tmap 지도 & 경로안내 API`) 키 발급
* [ ] **개발 환경 및 깃허브 저장소 구축**
  * Python 3.10+, PyTorch, OpenCV, FastAPI 가상환경 설정
  * Frontend: React (Vite) 또는 Vanilla HTML5/JS 환경 구성

### 💡 주의사항 (Pitfalls)
> 공공데이터 API 키는 발급 직후 약 1~2시간 동안 인증 에러(Service Key Not Registered)가 발생할 수 있으므로 프로젝트 시작 즉시 미리 신청해 두어야 합니다.

---

## 📌 [Phase 1] 기상 데이터 수집 및 위험 권역 트리거 구현 (2주차)

### 1. 주요 과제
* [ ] **위·경도 ↔ 기상청 격자(NX, NY) 변환 모듈 작성**
  * 기상청은 위경도 대신 고유의 LCC 투영 격자 좌표(`nx`, `ny`)를 사용하므로 좌표 변환 공식 함수 구현
* [ ] **초단기실황 파싱 모듈 구현**
  * 1시간 강수량(`RN1`), 강수형태(`PTY`: 0없음, 1비, 2비/눈, 3눈, 5빗방울) 데이터 추출
* [ ] **동적 트리거 엔진(Trigger Engine) 로직 구현**
  * 강수량 기준치(예: `RN1 >= 10.0mm/h`) 또는 강수형태에 따라 "현재 주의가 필요한 행정구역/지역 코드" 리스트를 발행

### 🎯 산출물
* `weather_collector.py`: 특정 위경도를 입력받아 현재 강수 여부를 판단하고 위험 레벨(정상/주의/경보)을 반환하는 스크립트

---

## 📌 [Phase 2] CCTV 스트림 수집 및 프레임 샘플링 모듈 (3주차)

### 1. 주요 과제
* [ ] **CCTV 메타데이터 필터링 모듈**
  * Phase 1에서 위험 지역으로 지정된 권역 내에 위치한 CCTV 목록(위·경도, 도로명, 스트리밍 URL) 추출
* [ ] **OpenCV 기반 스트림 디코딩**
  * 반환된 HLS(`.m3u8`) 또는 mp4 스트림 주소를 `cv2.VideoCapture`로 연결
* [ ] **프레임 샘플링(Sampling) 최적화**
  * 30fps 전체를 분석하지 않고, **5초 또는 10초에 1프레임**만 캡처하여 추론 큐(Queue)로 전달

### 💡 주의사항 (Pitfalls)
> 일부 공공 CCTV는 통신 불안정으로 인해 접속 타임아웃이 발생할 수 있습니다. `Timeout` 처리 및 연결 실패 시 대체할 수 있는 샘플 도로 영상(백업 mp4)을 반드시 로컬에 보관해 두어야 시연 시 중단되지 않습니다.

---

## 📌 [Phase 3] AI 객체 탐지(YOLO) 모델 학습 및 파인튜닝 (4주차)

### 1. 주요 과제
* [ ] **학습 데이터셋 수집 및 정제**
  * Roboflow Public Datasets 또는 AIHub(도로 노면 상태 파손 데이터셋) 활용
  * 클래스 정의 (현실적 3대 타깃):
    1. `Flooding` (도로 침수/대형 물웅덩이)
    2. `Pothole_Large` (대형 노면 파손)
    3. `Sinkhole` (도로 함몰)
* [ ] **YOLOv8 경량 모델 파인튜닝 (Fine-tuning)**
  * `yolov8n.pt` 또는 `yolov8s.pt` 기반 전이 학습
  * 입력 해상도 640x640, 우천 시 저시정 환경에 대응하도록 Data Augmentation (Brightness, Blur, Mosaic) 적용
* [ ] **추론 테스트 및 신뢰도 임계값(Threshold) 튜닝**
  * Confidence Score 0.6~0.7 이상일 때만 탐지 결과로 확정하도록 필터링

### 🎯 산출물
* `best.pt`: 파인튜닝 완료된 가중치 파일
* `detector.py`: 이미지 프레임을 입력받아 위험 객체 존재 여부와 바운딩 박스를 반환하는 모듈

---

## 📌 [Phase 4] 통합 백엔드 API 서버 및 공간 DB 구축 (5주차)

### 1. 주요 과제
* [ ] **데이터베이스 모델링 (SQLite 또는 PostgreSQL)**
  * `HazardEvent` 테이블 설계:
    * `id`: 고유키
    * `latitude`, `longitude`: 위험 지점 좌표
    * `hazard_type`: 위험 유형 (`FLOODING`, `POTHOLE`)
    * `confidence`: AI 확신도 (예: 0.87)
    * `cctv_name`: 감지된 CCTV 위치명
    * `detected_at`: 감지 일시
    * `is_active`: 활성 상태 플래그 (일정 시간 경과 후 만료 처리)
* [ ] **FastAPI 기반 RESTful API 엔드포인트 구현**
  * `GET /api/hazards`: 현재 활성화된 위험 지점 목록 조회
  * `POST /api/check-route`: 사용자의 경로 Polyline 상에 위치한 위험 지점 목록 반환
* [ ] **지오펜싱(Geo-fencing) 연산 구현**
  * 두 지점 간 거리를 계산하는 하버사인(Haversine) 공식 함수 작성

---

## 📌 [Phase 5] 웹 내비게이션 UI & 가상 주행 시뮬레이터 (6주차)

### 1. 주요 과제
* [ ] **지도 뷰 및 경로 탐색 UI (Kakao Map / Tmap)**
  * 출발지/도착지 검색 및 카카오/티맵 길찾기 API 연동
  * 추출된 Polyline 좌표열을 지도 위에 푸른색 경로선으로 렌더링
* [ ] **위험 지점 마커 및 위험 반경(100m) 시각화**
  * DB에 등록된 위험 지점에 붉은색 경고 마커와 반경 100m 원(Circle) 표시
* [ ] **가상 주행 시뮬레이터 (Virtual GPS Runner) 구현**
  * Polyline 좌표를 따라 자동차 마커가 1~2초 간격으로 전진하는 시뮬레이션 애니메이션 기능
* [ ] **실시간 위험 경고 시스템 (Audio + UI)**
  * 차량 마커가 위험 반경(100m 이내) 진입 감지 시:
    1. **Web Audio API**: 삐-삐- 비프음 재생
    2. **Web Speech API (TTS)**: *"전방 100미터 앞 도로 침수 위험 구역입니다. 서행하십시오."* 음성 출력
    3. **HUD 팝업**: 화면 상단에 붉은색 경고 카드 표출

---

## 📌 [Phase 6] 통합 테스트, 시연 시나리오 구축 & 최종 발표 준비 (7~8주차)

### 1. 주요 과제
* [ ] **E2E(End-to-End) 통합 시나리오 테스트**
  * `호우 감지` $\rightarrow$ `CCTV 영상 로드` $\rightarrow$ `AI 침수 감지` $\rightarrow$ `DB 등록` $\rightarrow$ `가상 주행 중 100m 앞 경고음/TTS 재생` 전 과정 검증
* [ ] **시연 안전장치(Demo Fail-safe / Mock Mode) 마련**
  * 발표 당일 비가 오지 않거나 공공 API 서버 장애가 발생해도 시연이 가능하도록 **"시뮬레이션 모드(사전 녹화된 우천 CCTV 영상 및 가상 기상 데이터 모드)"** 토글 스위치 구현
* [ ] **심사위원 질문 대비 Q&A 슬라이드 작성**
  * *"CCTV 해상도가 낮아서 작은 포트홀은 안 보이지 않나요?"* $\rightarrow$ 침수/대형파손 우선 타깃 및 고화질 스마트 관제망 확장성 답변
  * *"상용 티맵 앱에서 직접 소리가 나게 할 순 없나요?"* $\rightarrow$ 상용 앱 SDK 제약 설명 및 Polyline 기반 자체 웹 내비 HUD 엔진 구현 의의 강조
  * *"수많은 CCTV를 돌리면 서버 비용은 어떻게 감당하나요?"* $\rightarrow$ 기상청 호우 데이터 기반 3단계 이벤트 드리븐 필터링 구조 강조

---

## 📊 진행 관리 체크리스트 요약

| 단계 | 핵심 과제 | 주요 기술 / 라이브러리 | 완료 기준 |
| :---: | :--- | :--- | :---: |
| **Phase 0** | API 키 발급 & 환경 셋업 | 기상청, ITS, Kakao Developers | 모든 API 호출 테스트 200 OK |
| **Phase 1** | 기상 데이터 수집 모듈 | Python, Requests, LCC 좌표변환 | 강수량 기반 위험지역 판별 성공 |
| **Phase 2** | CCTV 영상 수집 파이프라인 | OpenCV, HLS/mp4 디코더 | 스트림에서 5초 주기 프레임 저장 |
| **Phase 3** | AI 객체 탐지 모델 파인튜닝 | Ultralytics YOLOv8, PyTorch | 침수/파손 mAP 0.75 이상 달성 |
| **Phase 4** | 백엔드 API & 위험 DB | FastAPI, SQLite/PostgreSQL | 위험 데이터 CRUD 및 공간 쿼리 완료 |
| **Phase 5** | 웹 내비 & 가상 주행 경고 | Kakao Maps SDK, Web Speech API | 100m 접근 시 TTS/경고 팝업 작동 |
| **Phase 6** | E2E 시연 준비 & 발표 자료 | Mock Fail-safe, PPT/데모 영상 | 끊김 없는 실시간 시연 시나리오 완성 |
